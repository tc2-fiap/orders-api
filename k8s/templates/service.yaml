apiVersion: v1
kind: Service
metadata:
  name: orders-api
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: orders-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
